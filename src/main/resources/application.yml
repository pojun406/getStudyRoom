# application.properties -> application.yml
spring:
  application:
    name: getStudyRoom

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:none} # ???? none, ???? ??? ?? ??
      show-sql: true

#jwt secret Key
jwt:
  secret:
    key: SSByZWFsbHkgd2FudCB0byBnZXQgYSBqb2IgYXQgU3luY0xpZmUK
